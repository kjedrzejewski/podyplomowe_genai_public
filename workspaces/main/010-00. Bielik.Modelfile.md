# Różnice między `010-00. Bielik.Modelfile` a oryginalnym Modelfile'em Bielika

Ten dokument wyjaśnia **co dokładnie zmieniamy** w naszym customowym Modelfile'u względem oryginalnego template'u modelu `SpeakLeash/bielik-11b-v3.0-instruct:bf16` z Ollama Hub. Wszystkie zmiany mają jeden cel: **sprawić, by Ollama wracała ustrukturyzowane `tool_calls` w odpowiedzi API**, zamiast wklejać tagi `<tool_call>{...}</tool_call>` jako zwykły tekst w polu `content`.

## Pełny oryginalny Modelfile dla porównania

Oryginalny Modelfile można podejrzeć komendą `ollama show --modelfile SpeakLeash/bielik-11b-v3.0-instruct:bf16`:

```
FROM /Users/.../models/blobs/sha256-b36c28be39b6b22a8acef89a9929150cbd9293eb34e008acdc4475880a8e691d
TEMPLATE """{{- /* SYSTEM + TOOLS INJECTION */ -}}
{{- if or .System .Tools -}}
<|im_start|>system
{{- if .System }}
{{ .System }}
{{- end }}

{{- if .Tools }}
You are provided with tool signatures that you can use to assist with the user's query.
You do not have to use a tool if you can respond adequately without it.
Do not make assumptions about tool arguments. If required parameters are missing, ask a clarification question.

If you decide to invoke a tool, you MUST respond with ONLY valid JSON in the following format:
{"name":"<tool-name>","arguments":{...}}

Below is a list of tools you can invoke (JSON):
{{ .Tools }}
{{- end }}
<|im_end|>
{{- end }}

{{- /* MESSAGES */ -}}
{{- range $i, $_ := .Messages }}
<|im_start|>{{ .Role }}
{{ .Content }}<|im_end|>
{{- end }}

{{- /* GENERATION PROMPT */ -}}
<|im_start|>assistant"""
PARAMETER stop <|start_header_id|>
PARAMETER stop <|end_header_id|>
PARAMETER stop <|eot_id|>
PARAMETER temperature 0.1
```

Niżej omawiam pięć zmian wprowadzonych w `010-00. Bielik.Modelfile`.

## TL;DR

| Aspekt | Oryginał | Nasz Modelfile |
|---|---|---|
| Format wywołania funkcji | `{"name":"…","arguments":{…}}` (sam JSON) | `<tool_call>{"name":"…","arguments":{…}}</tool_call>` (Hermes-style XML) |
| Iteracja `{{ .ToolCalls }}` w historii assistant | brak — tool calle nigdzie się nie pojawiają | jawna pętla z tagami `<tool_call>` przy zmiennej |
| Obsługa roli `tool` (wynik wywołania) | brak — generic `{{ .Role }}` | dedykowana sekcja z `<tool_response>...</tool_response>` |
| Stop sequences | tokeny **Llama 3** (`<|start_header_id|>`, `<|end_header_id|>`, `<|eot_id|>`) — niezgodne z tokenizerem Bielika | tokeny **ChatML** (`<|im_start|>`, `<|im_end|>`) — zgodne z tym, co model faktycznie emituje |
| Temperature | 0.1 | 0.1 (bez zmian) |

## Dlaczego to wszystko jest potrzebne?

Ollama nie ma pluginów Pythonowych do parsowania tool callów (jak vLLM). Zamiast tego ma **parser w Go**, który próbuje dynamicznie wykryć prefix wywołania funkcji **na podstawie templatu**:

> Parser wnioskuje prefix tagu tool calla z węzłów tekstowych przylegających do zmiennej `{{ range .ToolCalls }}` w templacie.

Jeśli template w ogóle nie iteruje `.ToolCalls` (jak w oryginale Bielika), parser nie wie, jakiego prefixu szukać w odpowiedzi modelu. Wynik: pole `tool_calls` w API zostaje puste, a Bielikowy `<tool_call>{...}</tool_call>` ląduje jako tekst w `content`.

## Zmiana 1: instrukcja systemowa dla narzędzi (`<tools>` + `<tool_call>`)

**Oryginał** (gdy dostarczono `.Tools`):

```
You are provided with tool signatures that you can use to assist with the user's query.
You do not have to use a tool if you can respond adequately without it.
Do not make assumptions about tool arguments. If required parameters are missing, ask a clarification question.

If you decide to invoke a tool, you MUST respond with ONLY valid JSON in the following format:
{"name":"<tool-name>","arguments":{...}}

Below is a list of tools you can invoke (JSON):
{{ .Tools }}
```

**Nasz Modelfile**:

```
You are a function calling AI model. You are provided with function signatures within <tools></tools> XML tags. You may call one function at a time to assist with the user query. Don't make assumptions about what values to plug into functions.

<tools>
{{- range .Tools }}
{{ . }}
{{- end }}
</tools>

For each function call, return a JSON object with function name and arguments within <tool_call></tool_call> XML tags as follows:
<tool_call>{"name": <function-name>, "arguments": <args-dict>}</tool_call>
```

**Co się zmieniło:**
- Definicje narzędzi opakowane w `<tools>...</tools>` (zamiast wymienione goło).
- Format wywołania funkcji opakowany w `<tool_call>...</tool_call>` (zamiast bezpośrednio JSON).

**Dlaczego:** Bielik był trenowany w stylu Hermes function calling — naturalnie emituje `<tool_call>{...}</tool_call>` niezależnie od tego, co mówi mu prompt systemowy. Nasza nowa instrukcja **odzwierciedla rzeczywisty zachowanie modelu**, zamiast walczyć z nim sugerując inny format.

## Zmiana 2: jawna pętla `{{ .ToolCalls }}` dla wiadomości `assistant`

**Oryginał:**

```jinja
{{- range $i, $_ := .Messages }}
<|im_start|>{{ .Role }}
{{ .Content }}<|im_end|>
{{- end }}
```

Wszystkie role (user, assistant, tool) są obsługiwane jednolicie — tylko `.Content`, brak iteracji `.ToolCalls`.

**Nasz Modelfile:**

```jinja
{{- range $i, $_ := .Messages }}
{{- $last := eq (len (slice $.Messages $i)) 1 -}}
{{- if eq .Role "user" }}<|im_start|>user
{{ .Content }}<|im_end|>
{{ else if eq .Role "assistant" }}<|im_start|>assistant
{{- if .Content }}
{{ .Content }}
{{- end }}
{{- if .ToolCalls }}
{{- range .ToolCalls }}<tool_call>{"name": "{{ .Function.Name }}", "arguments": {{ .Function.Arguments }}}</tool_call>
{{- end }}
{{- end }}{{ if not $last }}<|im_end|>
{{ end }}
{{- else if eq .Role "tool" }}<|im_start|>tool
<tool_response>
{{ .Content }}
</tool_response><|im_end|>
{{ end }}
{{- if and (ne .Role "assistant") $last }}<|im_start|>assistant
{{ end }}
{{- end }}
```

**Co się zmieniło:**
- Każda rola (`user`, `assistant`, `tool`) ma osobną gałąź.
- Dla `assistant` jest jawna iteracja `{{- range .ToolCalls }}<tool_call>{...}</tool_call>{{- end }}`.

**Dlaczego — to jest **klucz** całego rozwiązania:** parser Ollamy patrzy na tekst sąsiadujący ze zmienną `.ToolCalls` w templacie, żeby zorientować się, jakim prefixem model będzie oznaczał wywołania funkcji w odpowiedzi. Bez tej iteracji w templacie parser nie ma żadnej wskazówki — i nawet jeśli model wyemituje `<tool_call>...`, Ollama tego nie rozpozna i wrzuci do `content`.

## Zmiana 3: obsługa roli `tool` (wyniku wywołania funkcji)

**Oryginał:** brak rozróżnienia. Wynik wywołania funkcji (rola `tool` w API) zostałby sformatowany generycznie jako `<|im_start|>tool\n{wynik}<|im_end|>`.

**Nasz Modelfile:**

```jinja
{{- else if eq .Role "tool" }}<|im_start|>tool
<tool_response>
{{ .Content }}
</tool_response><|im_end|>
{{ end }}
```

**Co się zmieniło:** wynik wywołania opakowany w `<tool_response>...</tool_response>`.

**Dlaczego:** to konwencja Hermes function calling - model lepiej rozumie wyniki, gdy są wyraźnie oznaczone jako odpowiedź narzędzia. Bez tego model bywa zdezorientowany i może zignorować wynik albo halucynować, że narzędzie zwróciło coś innego.

## Zmiana 4: warunkowy generation prompt

**Oryginał:** zawsze dokleja `<|im_start|>assistant` na końcu — nawet jeśli ostatnia wiadomość już była od asystenta.

**Nasz Modelfile:**

```jinja
{{- if and (ne .Role "assistant") $last }}<|im_start|>assistant
{{ end }}
```

**Dlaczego:** w pętli function callingu zdarza się, że ostatnia wiadomość w historii to wynik narzędzia (`tool`) i wtedy chcemy `<|im_start|>assistant` jako prompt do generacji. Ale jeśli nasza jawna gałąź `assistant` wyżej już otworzyła sekcję `<|im_start|>assistant\n{content}<tool_call>...`, a my **nie chcemy** wtedy zamykać jej `<|im_end|>` (bo model dopiero ma kontynuować), to bez tego warunku doleciałyby dwa znaczniki `<|im_start|>assistant` — co model zinterpretuje chaotycznie.

## Zmiana 5: stop tokens (poprawka buga w oryginale)

**Oryginał:**

```
PARAMETER stop <|start_header_id|>
PARAMETER stop <|end_header_id|>
PARAMETER stop <|eot_id|>
PARAMETER temperature 0.1
```

**Nasz Modelfile:**

```
PARAMETER temperature 0.1

PARAMETER stop "<|im_start|>"
PARAMETER stop "<|im_end|>"
```

**Co się zmieniło:**
- Stop tokens: z **tokenów Llama 3** (`<|start_header_id|>`, `<|end_header_id|>`, `<|eot_id|>`) na **tokeny ChatML** (`<|im_start|>`, `<|im_end|>`).
- Temperature bez zmian (0.1) - dla agentów chcemy minimum kreatywności i deterministyczne wybory narzędzi.

**Dlaczego stop tokens:** to wygląda na **bug w oryginalnym Modelfile'u Bielika**. Bielik używa formatu ChatML (jego template jasno operuje na `<|im_start|>`/`<|im_end|>`), a stop tokens w oryginale to tokeny Llamy 3 — zupełnie innego modelu. Skutkuje to tym, że Ollama nie zatrzymuje generacji w odpowiednim miejscu, a model czasem produkuje śmieci poza pierwszym `<|im_end|>`. Zmiana na właściwe tokeny ChatML naprawia ten problem.

## Czego **nie** zmieniliśmy

- Bazy modelu — `FROM SpeakLeash/bielik-11b-v3.0-instruct:bf16`. Wagi się nie zmieniają, używamy tego samego pobranego pliku.
- Mechanizmu wstrzykiwania `.System` — gdy nie ma narzędzi, prompt wygląda dokładnie tak samo jak w oryginale.
- Formatu `<|im_start|>{role}\n{content}<|im_end|>` dla roli `user` (poza tym, że teraz jest w osobnej gałęzi `if`).

## Czy to gwarantuje, że tool_calls będą poprawnie zwracane?

**Nie.** Custom template *zwiększa szanse*, ale Bielik nadal bywa niespójny:

- Czasem emituje `<tool_call> {"name":...}` (z wiodącą spacją po tagu) — co rozjeżdża parser, który oczekuje `<tool_call>{`.
- Czasem zwraca dwa wywołania funkcji w jednej wiadomości - parser czasem wyciągnie tylko pierwsze.
- Czasem wciska tagi w środku swobodnego tekstu (`Sprawdzę pogodę: <tool_call>{...}</tool_call>`) — wtedy wszystko poza pierwszym tagiem trafia do `content`.

Jeśli zauważysz, że tool calle nadal lądują w `content` zamiast `tool_calls`, **to nie jest błąd templatu — to ograniczenie pary Bielik+Ollama**. Najpewniejsze ścieżki w takim wypadku:
- ReAct (notebooki `010-03. Bielik-dspy.ipynb` lub `010-04. Bielik-llama-index.ipynb`) - omija problem strukturyzowanego parsowania.
- vLLM z [`speakleash/bielik-tools`](https://github.com/speakleash/bielik-tools) - oficjalna ścieżka SpeakLeash do strukturyzowanego function callingu (wymaga GPU NVIDIA).

## vLLM jako alternatywa z bardziej tolerancyjnym parserem

Warto dopowiedzieć, **dlaczego** Modelfile w Ollamie nie wystarczy do pełnej niezawodności. Parser tool callów w Ollamie wyciąga prefix wywołania funkcji **literalnie** z tekstu sąsiadującego ze zmienną `{{ range .ToolCalls }}` i szuka go w odpowiedzi modelu jako dokładnej sekwencji bajtów. Nie ma tolerancji na warianty whitespace ani alternatywne formy. Sprawia to, że:

- Modelfile z prefixem `<tool_call>` łapie `<tool_call>{...`, ale **nie** złapie `<tool_call> {...` (ze spacją).
- Modelfile z prefixem `<tool_call> ` (ze spacją) łapie wariant ze spacją, ale **nie** złapie wariantu bez spacji.

Czyli nie da się obsłużyć obu form jednocześnie w samej warstwie templatu. Issue [`ollama/ollama#7014` "Better Tool Call parsing"](https://github.com/ollama/ollama/issues/7014) dopiero postuluje dodanie do Ollamy state-machine'owego matchera ze składnią typu `@<tool_call>@? @@json{name,args}@@` - na razie nie jest wdrożony.

**Czystszą drogą produkcyjną** dla tej samej rodziny modeli jest **vLLM** z natywnymi parserami tool callów per format. Dla Bielika (rodzina Hermes function calling) odpowiedni jest:

```bash
vllm serve speakleash/Bielik-11B-v3.0-Instruct \
    --tool-call-parser hermes \
    --enable-auto-tool-choice
```

Parser `hermes` w vLLM jest wyraźnie tolerancyjny na warianty whitespace i drobne wariacje formatowania w wyjściu modelu, więc to ścieżka, która domyka problem pominięty w Ollamie. SpeakLeash udostępnia gotową konfigurację w repo [`speakleash/bielik-tools`](https://github.com/speakleash/bielik-tools).

**Kiedy warto przełączyć:** gdy notebook lokalnie na Ollamie regularnie zwraca puste `tool_calls`, a aplikacja musi działać niezawodnie w produkcji. Wymaga GPU NVIDIA (typowo ≥ 24 GB VRAM dla wariantu 11B w bf16, mniej dla kwantyzowanego).
